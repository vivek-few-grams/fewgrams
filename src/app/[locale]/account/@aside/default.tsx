/** Nothing in the account column above the menu unless a page puts it there
 *  — see `@aside/orders/[id]`. Rendered on a full page load of any account
 *  page without its own `@aside` page. */
export default function AsideDefault() {
  return null;
}
